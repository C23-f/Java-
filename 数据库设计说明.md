# 城市社区 15 分钟生活圈便民设施评价与管理系统 —— 数据库设计说明

> 交付物：`init_db.sql`（单文件，一键建库建表 + 测试数据 + 视图 + 存储过程）
> 方案：**方案 A · 共享 SQL 脚本**——组员各自在本机 SSMS 打开脚本点执行，即可获得一模一样的数据库环境。

---

## 1. 设计目标

1. **一个脚本全搞定**：自动创建数据库 → 建表 → 建空间索引 → 灌测试数据 → 建视图/存储过程，全部在 `init_db.sql` 内，可重复执行（先删后建，刷新数据）。
2. **空间数据原生支持**：为后端空间分析模块（缓冲区生成、相交统计、距离计算、可达性评分）提供 SQL Server 原生 `geography` 空间类型与空间索引。
3. **覆盖全系统数据**：用户权限、小区、设施 POI、行政区划、评分结果、操作日志、原始数据暂存，共 9 张表。
4. **测试数据可直接演示**：2 个种子小区 + 1119 条爬虫采集的张店区真实 POI（银行 224、学校 59、商超 553、公园 58、公交站 225），覆盖 5 个设施大类，1000 米缓冲区分析可直接跑出真实结果。

## 2. 空间数据设计（关键）

| 设计点 | 选择 | 原因 |
|---|---|---|
| 空间类型 | `geography` | 存经纬度（球面坐标），适合真实城市点位 |
| 坐标系 | SRID = 4326（WGS84） | 高德/百度/谷歌等地图 API 通用经纬度 |
| 点位构建 | `geography::Point(纬度, 经度, 4326)` | **注意参数顺序：先纬度后经度** |
| 缓冲区 | `location.STBuffer(米)` | geography 的缓冲区单位为**米**，`STBuffer(1000)` = 1000 米 |
| 相交判断 | `location.STIntersects(@缓冲区)` | 判断设施是否落在生活圈内 |
| 距离计算 | `geo1.STDistance(geo2)` | 返回米制距离 |
| 空间索引 | `CREATE SPATIAL INDEX ...` | 加速缓冲区/相交/距离查询（每表每空间列限 1 个） |
| 冗余经纬度 | 各表同时存 `longitude/latitude` | Excel 导入导出、前端列表、调试方便 |

**15 分钟生活圈半径的取值依据**：成年人步行速度约 1.2 m/s，15 分钟可走约 1080 米。系统默认缓冲区半径取 **1000 米**（`sp_community_15min_stats`、`sp_calc_accessibility` 的 `@radius_m` 参数可随时调整）。

## 3. 表结构总览

| 表名 | 说明 | 关键字段 |
|---|---|---|
| `sys_role` | 角色表 | role_code / role_name |
| `sys_user` | 用户表（登录权限） | username / password(哈希) / role_id / status |
| `region` | 行政区划表 | region_name / parent_id / region_level / **geom(面)** |
| `community` | 居住小区表 | community_name / **location(点)** / region_id / house_count |
| `facility_category` | 设施分类表 | category_code / **weight(评分权重)** / sort_order |
| `facility` | 便民设施 POI 表 | facility_name / category_id / **location(点)** / poi_id(去重) |
| `accessibility_score` | 可达性评分结果表 | community_id / total_score / score_level / score_detail(JSON) |
| `operation_log` | 操作日志表 | user_id / action / module |
| `facility_staging` | 原始 POI 暂存表 | raw_json / process_status（数据处理用） |

## 4. 表关系（ER）

```
sys_role 1 ──── * sys_user            （角色 → 用户）
region   1 ──── * community           （片区 → 小区）
region   1 ──── * region              （自关联：省→市→区县→片区）
facility_category 1 ──── * facility   （分类 → 设施）
community 1 ──── * accessibility_score（小区 → 历次评分）
sys_user  1 ──── * operation_log      （用户 → 操作日志）
```

## 5. 可达性评分模型（参考实现，存储过程 `sp_calc_accessibility`）

- 对 7 大类设施**逐类**统计缓冲区内的数量 `cnt`；
- 每类得分 = `min(cnt, 上限5) / 5 × 100`（**某类缺失按 0 分计，纳入惩罚**）；
- 综合分 = `Σ(weight × 类得分) / Σ(weight)`，权重在 `facility_category.weight` 配置；
- 等级：`≥85 优，≥70 良，≥55 中，其余 差`；
- 结果写入 `accessibility_score`，同时输出分类明细 JSON（前端 ECharts 可直接用）。

示例（1 号小区，1000 米缓冲区，含爬虫真实数据实测）：

| 指标 | 值 |
|---|---|
| 综合评分 | 61.16（中） |
| 缓冲区内设施总数 | 168 |
| 覆盖设施类别 | 6（养老助残为 0） |
| 分类明细 | EDU:3, MED:1, MKT:105, CUL:3, LIFE:20, AGE:0, TRA:36 |

> 全库共 1121 条设施（2 条种子 + 1119 条爬虫 POI）。

## 6. 常用视图 / 存储过程

| 对象 | 说明 |
|---|---|
| `v_facility_category` | 设施 + 分类联查（前端列表 / GeoJSON 输出） |
| `v_community_score` | 小区 + 最近一次评分 |
| `sp_community_15min_stats` | 指定小区生成缓冲区，返回各分类设施数量 + 总数 |
| `sp_calc_accessibility` | 可达性加权评分，写库并返回结果 |

## 7. 使用方式

1. 用 SSMS 打开 `init_db.sql`；
2. 点“执行”（F5）；
3. 完成后可执行示例：

```sql
USE LifeCircleDB;
EXEC dbo.sp_calc_accessibility @community_id = 1, @radius_m = 1000;   -- 评分
EXEC dbo.sp_community_15min_stats @community_id = 1, @radius_m = 1000; -- 分类统计
SELECT * FROM dbo.v_community_score;   -- 小区+评分
```

测试账号：`admin / operator`，密码均为 `123456`（存 SHA2_256 哈希）。

## 8. 已验证

在本地 SQL Server 2022 实测执行：**30 个批次全部成功，0 失败**；脚本可重复执行（自动清理重建）。全库 1121 条设施、2 个小区，缓冲区分析与评分存储过程运行正常。

## 8.1 数据说明

- **爬虫 POI 坐标**：来自高德地图 API，坐标系为 **GCJ-02（火星坐标）**。前端最终在高德地图展示，高德 JS API 原生使用 GCJ-02，因此坐标值可直接使用，**无需做任何坐标系转换**。数据库中 geography 字段标注 SRID 4326 仅为 SQL Server 类型要求，不影响前端高德地图渲染。
- **facility.address 列**：因公交站的"地址"字段存储公交线路列表（最长 416 字符），已从 `NVARCHAR(200)` 扩为 `NVARCHAR(500)`，属安全扩列，不影响现有接口读取。
- **poi_id 命名**：种子数据为 `POI2026xxxxxx`，爬虫数据为 `POI_BANK_xxxx / POI_SCH_xxxx / POI_MKT_xxxx / POI_PARK_xxxx / POI_BUS_xxxx`，全局唯一，配合过滤唯一索引实现去重。

## 9. 可定制点

- **城市 / 片区 / 小区 / 设施名称**：修改测试数据段即可；
- **分类与权重**：改 `facility_category` 的 `weight`（影响评分）；
- **缓冲区半径**：改存储过程入参 `@radius_m`（默认 1000 米）；
- **评分模型**：如小组讨论出更细的加权模型，改 `sp_calc_accessibility` 的得分公式即可，表结构无需变动；
- **Java 侧**：数据库工程师可通过 JDBC 执行空间 SQL（`STBuffer`/`STIntersects`/`STDistance`），或直接调用上述存储过程。

