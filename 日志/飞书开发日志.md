## [2026‑09‑04 T13 修复POI爬虫分类过滤与清洗输出]

- **日期时间：** 2026‑09‑04
- **操作人：** 开发者
- **模块：** 数据采集层、数据清洗层
- **做了什么修改：**
  （1）修正学校类高德POI分类编码`types`，由错误的`"|141202|141203|141204"`改为`"141201|141202|141204"`，严格限定只抓取小学、中学、高等院校三类；
  （2）删除`data/raw`目录下旧的原始CSV文件，重新运行爬虫采集，确保新的`types`分类编码生效；
  （3）重写清洗端过滤逻辑，将`apply(axis=1)`自定义函数改为`str.contains`布尔索引，直接匹配`类型`字段，提升过滤可靠性；
  （4）清洗输出由合并单一文件改为**按类别分开输出**，在`data/raw`目录下生成`poi_学校_cleaned.csv`、`poi_医院_cleaned.csv`等6个分类清洗文件；
  （5）清洗读取时自动跳过`_cleaned`后缀文件，避免重复清洗。
- **尝试的实现方法：**
  先在爬虫端通过高德`types`参数做分类粗筛，再在清洗端用`pandas.str.contains`做关键词黑名单精筛，双重过滤确保噪声点位被剔除。
- **遇到的问题：**
  （1）修改`types`编码后幼儿园仍然存在，排查发现是旧CSV文件未被覆盖，查看的仍是历史数据；
  （2）原代码学校类`types`编码开头多写了一个`|`分隔符，且漏掉小学编码`141201`、误包含职校编码`141203`；
  （3）清洗端使用`apply(axis=1)`自定义函数过滤，在当前环境下未正常生效，幼儿园点位未被剔除；
  （4）原清洗脚本将所有类别合并输出为单个`poi_cleaned.csv`，无法按类别单独使用。
- **解决方案：**
  （1）手动删除`data/raw`下全部旧文件后重新爬取，使新分类编码生效；
  （2）修正`types`编码为`"141201|141202|141204"`，对应小学、中学、大学；
  （3）清洗端改用`str.contains`布尔索引替代`apply`，逐关键词打印剔除数量，确保过滤可观测；
  （4）按`类别`字段分组后分别输出为`poi_{类别}_cleaned.csv`，保留分类结构。
- **创新点：**
  爬虫端`types`粗筛与清洗端关键词精筛双重结合；清洗结果按原始类别拆分输出，既保留分类结构又便于后续按类别单独导入GIS软件分析。
- **测试记录：**
  重新爬取后学校类原始数据中幼儿园数量为0；清洗脚本控制台正常打印各类别剔除数量；`data/raw`目录下生成6个`_cleaned.csv`文件，Excel打开中文无乱码，学校类仅含小学、中学、高等院校。
- **关联提交/文件：**
  `amap_poi_crawler.py`、`data/raw/poi_学校.csv`、`data/raw/poi_学校_cleaned.csv`、`data/raw/poi_医院_cleaned.csv`
  
## [2026-09-04 T12 后台登录页面开发与后台框架确认]
- **日期时间：** 2026-09-04
- **操作人：** 陈馨妍
- **模块：** 前端业务
- **做了什么修改：**
  （1）编写登录页面 main.html，完成登录页面布局、基础样式；
  （2）梳理确认后台管理系统整体搭建要求，确定后台导航、各功能页面划分；
  （3）完成 main.html 登录界面代码编写，将文件上传至项目仓库。
- **尝试的实现方法：**
  使用 HTML + CSS + Element Plus 完成登录页面 UI 布局，规划后台整体页面结构，划分小区管理、设施管理、账号管理、统计分析四大功能模块，为后续各个业务页面开发做好前期准备。
- **遇到的问题：**
  （1）Element Plus 组件引入初期页面样式不生效；
  （2）git 切换分支时本地找不到远程新建分支，无法切换到个人开发分支。
- **解决方案：**
  （1）核对 CDN 引入链接，确认 css、js 资源完整引入，页面样式恢复正常；
  （2）执行`git fetch origin`拉取远程分支信息，成功切换到个人开发分支，完成代码提交上传。
- **创新点：**
  无
- **测试记录：**
  VSCode 打开 main.html，浏览器可以正常打开登录页面，页面布局展示正常，基础组件渲染无误。
- **关联提交/文件：**
- `main.html

## [2026‑09‑04 T11 项目版本管控、接口基础测试、进度跟踪]
- **日期时间：** 2026‑09‑04
- **操作人：** 张佳丽
- **模块：** 项目管控与交付模块
- **做了什么修改：**
  （1）巡检GitHub仓库，检查各成员代码提交、日志提交情况，核对前后端、数据处理、数据库模块的提交文件；
  （2）对张喜浩开发的`/api/category/list`设施分类接口做基础接口测试，校验JSON返回格式、code、data字段；
  （3）收集今日各组员开发日志，检查日志完整性，确认每个人工作痕迹留存；提醒本周五下午前各岗位输出负责功能图表并发送项目群；
  （4）跨岗位对齐依赖关系：前端GIS模块依赖后端分类接口、空间分析模块依赖数据库初始化脚本，同步组员模块依赖顺序；
  （5）检查仓库`.gitignore`、目录结构，确认target、编译产物、IDE配置文件没有被误提交；
  （6）梳理当前项目待办清单：单元&自动化测试用例编写、每周主干合并准备、答辩材料前期素材收集。
- **尝试的实现方法：**
  使用git status、git log查看仓库提交记录；浏览器直接访问后端接口做简易冒烟测试；统一对照【开发日志填写规范.md】检查组员日志格式；梳理模块依赖清单用于小组内部同步。
- **遇到的问题：**
  （1）部分组员推送GitHub遇到网络问题，出现443连接失败、推送超时；
  （2）部分模块之间存在依赖，开发顺序容易混乱，组员不清楚自己模块需要等待哪些前置工作完成；
  （3）个别日志缺少关联提交/文件字段，日志信息不全。
- **解决方案：**
  （1）给到网络问题备选方案：切换移动网络、使用Watt Toolkit做GitHub加速，规避网络波动；
  （2）整理简易依赖说明，在群内同步：数据库脚本优先执行 → 后端接口开发 → 前端对接；
  （3）对照开发日志规范提醒组员补全日志关联文件信息，保证日志可追溯。
- **创新点：**
  建立每日仓库巡检机制，提前发现代码提交、日志记录问题，降低后期合并代码冲突风险。
- **测试记录：**
  访问`http://localhost:9090/api/category/list`，接口返回code=200，返回JSON结构符合项目统一Result<T>封装；GitHub仓库目录结构正常，backend、frontend、日志目录文件完整；组员开发日志全部归档。
- **关联提交/文件：**
  `日志/飞书开发日志.md`、`开发日志填写规范.md`
  
## [2026-09-04 T10 完成前端GIS模块初始化]
- **日期时间：** 2026-09-04
- **操作人：** 尹晓晗
- **模块：** 前端页面
- **做了什么修改：**
  （1）基于 Leaflet 完成用户端地图主界面开发，搭建页面布局，实现顶部操作栏、右侧统计面板、地图容器页面结构；
  （2）完成天地图底图接入，加载矢量底图与地名注记图层，完成淄博市地图视图初始化；
  （3）分层渲染小区、便民设施 POI 点位，设置差异化标记符号。
- **尝试的实现方法：**
  引入 Leaflet 库构建地图实例，拼接天地图瓦片服务地址加载底图；使用 L.geoJSON 解析空间要素，pointToLayer 自定义点符号，onEachFeature 绑定弹窗信息。
- **遇到的问题：**
  （1）天地图瓦片加载空白，密钥类型选择错误；
  （2）POI 点位经纬度顺序颠倒，点位地图位置发生偏移；
  （3）图层切换时图层重复添加、移除逻辑混乱。
- **解决方案：**
  （1）更换为 Web 浏览器类型天地图 token，同时加载矢量底图与注记两层瓦片；
  （2）严格遵循 Leaflet [纬度，经度] 的坐标顺序，修正 GeoJSON 坐标字段；
  （3）通过独立图层实例管理小区、设施图层，增加判断避免重复 addTo。
- **创新点：**无
- **测试记录：**
  本地 LiveServer 运行，底图正常加载；小区、便民设施 POI 正常分层显示。
- **关联提交/文件：**

## [2026-09-04 T9 设施分类列表查询接口开发]
- **日期时间：** 2026-09-04
- **操作人：** 张喜浩
- **模块：** 后端接口
- **做了什么修改：**
  （1）新建 common 包，编写 Result<T> 统一返回格式类，封装 code、message、data 三个字段，提供 success() 和 error() 静态方法；
  （2）新建 entity 包，编写 FacilityCategory 实体类，对应数据库 facility_category 表，使用 Lombok @Data 注解简化 getter/setter；
  （3）新建 mapper 包，编写 FacilityCategoryMapper 接口，定义 selectAll() 查询方法；
  （4）在 resources/mapper 目录下编写 FacilityCategoryMapper.xml，编写查询所有设施分类的 SQL 语句，按 sort_order 排序；
  （5）新建 service 包及 impl 子包，编写 FacilityCategoryService 接口及 FacilityCategoryServiceImpl 实现类，调用 Mapper 层完成数据查询；
  （6）新建 controller 包，编写 FacilityCategoryController，提供 GET /api/category/list 接口，返回设施分类列表的 JSON 数据。
- **尝试的实现方法：**
  采用 Spring Boot + MyBatis 标准三层架构（Controller -> Service -> Mapper），MyBatis 使用 XML 方式编写 SQL，数据库下划线字段通过 application.yml 中 map-underscore-to-camel-case 配置自动映射为 Java 驼峰命名；统一返回结果使用 Result<T> 封装。
- **遇到的问题：**
  （1）首次启动时 MyBatis 报"No MyBatis mapper was found"警告，原因是还未编写 Mapper 接口和 XML；
  （2）VS Code 终端执行 mvn 命令时报"无法将mvn识别为cmdlet"，原因是配置 Maven 环境变量前已打开的终端未刷新 PATH。
- **解决方案：**
  （1）按标准结构编写 Mapper 接口与对应 XML 文件，确保 namespace 与接口全限定名一致、id 与方法名一致，启动后警告消失；
  （2）关闭旧终端重新打开，或在当前终端手动执行 env:Path 刷新命令加载最新环境变量。
- **创新点：**
  无
- **测试记录：**
  启动后端服务后，浏览器访问 http://localhost:9090/api/category/list，返回 code=200，data 包含7条设施分类记录（EDU教育设施、MED医疗卫生、MKT商业服务、CUL文体活动、LIFE生活服务、AGE养老助残、TRA公共交通），数据库查询链路正常。
- **关联提交/文件：**
  `backend/src/main/java/com/example/springboot/common/Result.java`
  `backend/src/main/java/com/example/springboot/entity/FacilityCategory.java`
  `backend/src/main/java/com/example/springboot/mapper/FacilityCategoryMapper.java`
  `backend/src/main/java/com/example/springboot/service/FacilityCategoryService.java`
  `backend/src/main/java/com/example/springboot/service/impl/FacilityCategoryServiceImpl.java`
  `backend/src/main/java/com/example/springboot/controller/FacilityCategoryController.java`
  `backend/src/main/resources/mapper/FacilityCategoryMapper.xml`

  
## [2026‑09‑04 T8 POI 原始数据采集与任务一清洗开发]

- **日期时间：** 2026‑09‑04
- **操作人：** 刘广家
- **模块：** 数据采集层、数据清洗层‑任务一
- **做了什么修改：**
（1）编写高德 POI 爬虫脚本`amap_poi_crawler.py`，配置张店区行政编码、高德密钥，完成学校、医院、商超、公交站、公园、银行六类 POI 点位的分页采集；
（2）爬虫端增加网络异常捕获、同类别多关键词自动去重，原始结果导出至`data/raw`文件夹；
（3）新建任务一独立清洗脚本，对下载完成的原始 CSV 数据集执行整体去重、经纬度范围粗筛选；
（4）基于高德`类型`三级分类字段，剔除医院大类下停车场、住宅小区、餐饮商铺等混入噪声，剔除学校大类下培训机构、政府机关、疗养院等无关点位；
（5）清洗后数据集导出保存至`data/clean`，生成清洗完成文件`poi_cleaned.csv`。
- **尝试的实现方法：**
使用 Python+requests 调用高德地图 Web 服务 API 分页获取 POI 数据；借助 pandas 完成原始数据合并、去重与噪声过滤；爬虫采集、数据清洗代码文件分离，原始数据与清洗后数据分文件夹存储。
- **遇到的问题：**
多关键词检索时，同一 POI 点位会被不同关键词重复抓取返回；学校、医院分类下混入大量不属于目标类别的噪声 POI，不能依靠高德检索关键词自动过滤。
- **解决方案：**
在爬虫保存阶段以名称 + 经度 + 纬度作为唯一标识去除重复点位；在任务一清洗脚本中通过字符串匹配`类型`字段，精准剔除勾选的无关噪声项，医疗类内部诊所、药房等点位予以保留不删除。
- **创新点：**
爬虫与清洗流程解耦，原始采集数据永久留存，筛选规则可随时修改重跑；噪声过滤规则可视化对照属性统计表，筛选结果可控可追溯。
- **测试记录：**
爬虫可正常分页下载张店区六类 POI 数据；去重、经纬度过滤、噪声剔除功能运行正常；导出 CSV 文件用 Excel 打开中文无乱码。
- **关联提交 / 文件：**
`amap_poi_crawler.py`、`poi_clean_task1.py`、`data/raw/`、`data/clean/`


## [2026-09-03 T7 后端空间分析模块基础工程搭建]
- **日期时间：** 2026-09-03
- **操作人：** 孔俊童
- **模块：** 后端-空间分析模块
- **做了什么修改：**
  （1）完成开发环境配置：安装并配置 JDK 21 与 Maven 3.9.16，版本与团队主工程完全对齐；
  （2）搭建 Spring Boot 基础工程：在 backend 目录下创建标准 Maven 项目结构，包路径 com.example.springboot 与主工程保持一致，保障后续无缝合并；
  （3）完成核心配置编写：编写 pom.xml 对齐 Spring Boot 4.0.8、MyBatis 4.0.1、SQL Server 官方驱动等依赖版本；编写 application.yml 配置服务端口、数据库连接、MyBatis 映射规则；编写项目启动类并配置 Mapper 扫描规则；
  （4）项目启动验证：成功启动 Spring Boot 服务，Tomcat 在 9090 端口正常运行，持久层框架加载无报错；
  （5）Git 仓库规范整理：添加 .gitignore 过滤规则，移除 target 编译缓存目录，规范仓库提交内容。
- **尝试的实现方法：**
  采用 Maven 父工程继承方式，直接继承 Spring Boot 官方父工程统一管理依赖版本，避免版本冲突；空间模块独立分层封装，包结构、配置规范完全对齐主工程，后续可直接复制合并。
- **遇到的问题：**
  （1）国内网络访问 GitHub 不稳定，多次出现连接重置、443 端口连接失败，配置代理后仍无法连通；
  （2）初始提交未配置 .gitignore，将 target 编译缓存目录一并提交，造成仓库结构冗余。
- **解决方案：**
  （1）切换手机移动网络重试推送，成功完成代码同步；
  （2）执行 git rm -r --cached 移除已追踪的 target 目录，添加 .gitignore 规则，过滤编译产物、IDE 配置等非源码文件。
- **创新点：**
  严格遵循团队工程规范，依赖版本、包路径、配置规则与主工程 100% 对齐，空间模块独立分层封装，后续可直接无缝合并到主工程。
- **测试记录：**
  执行 mvn spring-boot:run 启动项目，Tomcat 成功监听 9090 端口，Spring Boot 与 MyBatis 框架初始化无报错，基础服务链路运行正常。
- **关联提交/文件：**
  `backend/pom.xml`
  `backend/src/main/java/com/example/springboot/SpringbootSpatialApplication.java`
  `backend/src/main/resources/application.yml`
  `.gitignore`


## [2026-09-03 T6 项目仓库环境搭建]
- **日期时间：** 2026-09-03
- **操作人：** 孔俊童
- **模块：** 文档层、仓库配置
- **做了什么修改：**
  （1）确定项目主题：城市社区15分钟生活圈便民设施评价与管理系统；
  （2）恢复误删除的【日志】文件夹，用于存放小组每日开发日志；
  （3）新建backend、frontend目录，增加.gitkeep占位文件，实现空目录提交GitHub；
  （4）配置本地git仓库user.name、user.email，解决commit提交报错；
  （5）梳理仓库目录规范，制定小组GitHub协作规则。
- **尝试的实现方法：**
  使用git restore从历史提交恢复误删文件夹；使用.gitkeep占位文件解决Git不支持空文件夹问题；仓库局部配置git账号信息，不影响电脑其他项目；要求组员开发前先pull同步主分支，完成任务后add‑commit‑push提交代码与文档。
- **遇到的问题：**
  （1）本地操作失误，误删除“日志”文件夹；
  （2）git commit时报错，未检测到邮箱地址，无法完成提交；
  （3）backend、frontend为空文件夹，Git不会追踪，无法上传到GitHub。
- **解决方案：**
  （1）使用git restore HEAD -- "日志"从提交历史恢复整个文件夹；
  （2）在本仓库内配置user.name、user.email，完成身份校验；
  （3）在backend、frontend内部新建.gitkeep占位文件，让空目录可以被git识别并推送。
- **创新点：**
  完整留存仓库搭建踩坑全过程，规范目录划分，前后端分离目录提前规划，便于7人小组分工协作，所有进度文档统一归档。
- **测试记录：**
  本地git status检查工作区干净；推送后GitHub网页可查看backend、frontend、日志目录及内部文件。
- **关联提交/文件：**
  `backend/.gitkeep`
  `frontend/.gitkeep`
  `日志/2026‑09‑03‑仓库搭建日志.md`

## [2026-09-03 T5 创建Spring Boot后端项目并配置SQL Server]
- **日期时间：** 2026-09-03
- **操作人：** 张喜浩
- **模块：** 后端接口
- **做了什么修改：**
  （1）在 IDEA 中创建 Spring Boot 项目（位于 D:\sxsxsx\vue-lx\springboot），Spring Boot 版本 4.0.8，Java 21，添加 Spring Web、MyBatis Framework 依赖；
  （2）将 application.yml 数据库配置从 MySQL 改为 SQL Server（驱动 com.microsoft.sqlserver.jdbc.SQLServerDriver、用户名 sa、密码 1、端口 1433、数据库 xm-pro、encrypt=false、trustServerCertificate=true）；
  （3）将 pom.xml 中 MySQL 驱动 mysql-connector-j 替换为 SQL Server 官方驱动 mssql-jdbc。
- **尝试的实现方法：**
  参照教程第8章"从0带你搭建SpringBoot3工程"，结合实际使用的 SQL Server 数据库进行配置适配。
- **遇到的问题：**
  创建项目时选 Spring Boot 4.1.1 导致 MyBatis Framework 选项灰色不可选（MyBatis 兼容范围 >=4.0.0 and <4.1.0-M1）；原配置为 MySQL 与实际 SQL Server 不匹配导致启动失败。
- **解决方案：**
  改用 Spring Boot 4.0.8 版本使 MyBatis 4.0.1 可正常添加；修改 application.yml 和 pom.xml 适配 SQL Server。
- **创新点：** 无
- **测试记录：** 配置文件修改完成，待 SQL Server 服务启动后验证连接。
- **关联提交/文件：**
  `springboot/src/main/resources/application.yml`
  `springboot/pom.xml`

  ## [2026-09-03 T4 完成数据库设计与初始化脚本]
- **日期时间：** 2026-09-03
- **操作人：** 郭德宽
- **模块：** 数据层
- **做了什么修改：**
（1）设计全系统 9 张数据表结构，含空间字段 geography（SRID 4326）与空间索引；
（2）编写 init_db.sql 一键初始化脚本，自动建库、建表、建索引、灌测试数据、建视图与存储过程；
（3）编写可达性加权评分存储过程 sp_calc_accessibility 与缓冲区分类统计存储过程 sp_community_15min_stats；
（4）测试数据精简为 2 个小区、2 个设施（种子数据），后续补充；
（5）编写数据库设计说明文档。
- **尝试的实现方法：**
采用方案 A 共享 SQL 脚本，单文件 init_db.sql 提交 GitHub，组员本地 SSMS 执行即可获得一致数据库环境；空间数据使用 SQL Server 原生 geography 类型，STBuffer 生成米制缓冲区，STIntersects 判断相交，STDistance 计算距离。
- **遇到的问题：**
（1）显式插入 IDENTITY 主键时报"IDENTITY_INSERT 设置为 OFF"错误；
（2）数据插入与 CREATE VIEW 混在同一批次，导致"CREATE VIEW 必须是查询批次中第一个语句"；
（3）清理段漏删 v_community_score 视图，重复执行时报对象已存在。
- **解决方案：**
（1）对 region / community / facility 表在插入前后加 SET IDENTITY_INSERT ON / OFF；
（2）数据插入段末尾补 GO 分隔批次；
（3）清理段补充 IF OBJECT_ID('dbo.v_community_score','V') IS NOT NULL DROP VIEW。
- **创新点：**
评分模型中缺失设施类别按 0 分计入惩罚（而非忽略），使可达性评分更真实；分类明细以 JSON 存储，前端 ECharts 可直接消费。
- **测试记录：**
本机 SQL Server 2022 实测执行 init_db.sql，29 个批次全部成功、0 失败；1 号小区 1000 米缓冲区分析正常返回（2 个设施在圈内，覆盖 2 类，评分 6.38）；组员本机 SSMS 执行同样成功，9 张表全部创建。
- **关联提交/文件：**
`init_db.sql`
`数据库设计说明.md`
  
  
## [2026‑09‑02 T3 完善README并添加仓库内部跳转链接]
- **日期时间：** 2026‑09‑02
- **操作人：** 后端空间分析模块-孔俊童
- **模块：** 文档层
- **做了什么修改：**
  （1）完善仓库README.md项目介绍；
  （2）增加可点击内部跳转链接，直接跳转到日志与规范文档。
- **尝试的实现方法：**
  使用Markdown内部相对路径链接，实现仓库内文件一键跳转，方便组员快速查阅文档。
- **遇到的问题：**
  只写文件名文本，无法点击跳转，组员查找文件不方便。
- **解决方案：**
  使用`[显示文字](文件路径)`格式编写可点击链接。
- **创新点：**
  仓库首页即可直达核心协作文档，降低组员查找成本。
- **测试记录：**
  刷新页面验证链接跳转效果。
- **关联提交/文件：**
  `README.md`

## [2026‑09‑02 T2 新增开发日志填写规范文档]
- **日期时间：** 2026‑09‑02
- **操作人：** 后端空间分析模块-孔俊童
- **模块：** 文档层
- **做了什么修改：**
  （1）新建`开发日志填写规范.md`，统一组员的日志书写标准；
  （2）把规则和实际任务日志文件拆分，方便组员查阅。
- **尝试的实现方法：**
  将规则单独抽离成独立文档，业务日志文件只保留任务记录，结构更清晰。
- **遇到的问题：**
  如果规则和日志混在同一个文件，内容会很长，不方便组员查看任务记录。
- **解决方案：**
  拆分成两个文件，一个放规范，一个专门记录任务。
- **创新点：**
  文档职责分离，便于团队协作。
- **测试记录：**
  GitHub网页可以正常渲染规范文档。
- **关联提交/文件：**
  `开发日志填写规范.md`
  
## [2026‑09‑02 T1 创建开发日志文档]
- **日期时间：** 2026‑09‑02 20：47
- **操作人：** 孔俊童
- **模块：** 文档层
- **做了什么修改：**
  （1）仓库根目录新建本日志文档，用来留存开发过程；
  （2）学习GitHub网页创建Markdown文件，理解`.md`后缀的作用。
- **尝试的实现方法：**
  使用Markdown格式维护开发日志，后续每次开发改动都在此文件追加记录。
- **遇到的问题：**
  GitHub网页新建文件时，如果不写`.md`后缀，就不会渲染标题列表格式。
- **解决方案：**
  创建文件时，文件名末尾手动加上`.md`。
- **创新点：**
  留存开发过程、踩坑记录，方便后续复盘回顾。
- **测试记录：**
  GitHub网页可以正常渲染本Markdown文档格式。
- **关联提交/文件：**
  `feishu-log.md`




